const Button = (props) => {
  const btnClassName = props.className + "job-btn";
  return (
    // <button key={props.key} className={btnClassName}>
    //   {props.children}
    // </button>
    <div></div>
  );
};

export default Button;
